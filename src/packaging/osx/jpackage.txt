--verbose
--name "${app.name}"
--app-version ${app.version.macos}
--type pkg
--dest "${project.build.directory}/installer-${os.detected.name}"
--main-class ${main-class}
--main-jar ${project.build.finalName}-jar-with-dependencies.jar
--input "${project.build.directory}/jars"
--runtime-image "${project.build.directory}/jvm-image"
--temp "/tmp/lens-correct-installer-${project.version}"
--resource-dir "${project.build.directory}/packaging/osx"
--java-options "-Djava.awt.headless=true"
--java-options "--add-opens=java.desktop/javax.swing=ALL-UNNAMED"
--java-options "--add-opens=java.base/java.lang=ALL-UNNAMED"
--java-options "--add-opens=java.base/java.io=ALL-UNNAMED"
--vendor "Janelia Saalfeld Lab"
--description "${project.description}"
--copyright "GPL v3"
--mac-package-identifier "org.janelia.saalfeldlab.lenscorrect"
--mac-package-name "${app.name}"